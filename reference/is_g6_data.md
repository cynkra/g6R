# Check if an object is a g6_data object

Check if an object is a g6_data object

## Usage

``` r
is_g6_data(x)
```

## Arguments

- x:

  An object to check.

## Value

Logical indicating if `x` is of class `g6_data`.
