% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plugins.R
\name{g6_outline}
\alias{g6_outline}
\title{Outline panel listing the graph}
\usage{
g6_outline(
  key = "outline",
  title = "Outline",
  position = c("top-right", "top-left", "bottom-left", "bottom-right"),
  width = 260,
  open = TRUE,
  groupsOpen = TRUE,
  expandAncestors = TRUE,
  select = TRUE,
  labels = c(node = "node", combo = "combo", edge = "edge"),
  animation = NULL,
  outputId = NULL,
  ...
)
}
\arguments{
\item{key}{Unique identifier for the plugin (string).}

\item{title}{Text on the panel's toggle button.}

\item{position}{Corner to render in: \code{"top-right"} (default), \code{"top-left"},
\code{"bottom-left"} or \code{"bottom-right"}.}

\item{width}{Width of the panel, in px.}

\item{open}{Start with the panel open.}

\item{groupsOpen}{Start with the groups unfolded.}

\item{expandAncestors}{Expand collapsed combos on the way to a clicked
element, as \code{\link[=g6_search]{g6_search()}} does.}

\item{select}{Also select the clicked element, so anything driven by selection
follows the panel.}

\item{labels}{How to name each element type, as a named character vector over
\code{"node"}, \code{"combo"} and \code{"edge"}. Only shown where a row has no group to show
instead.}

\item{animation}{Viewport animation passed to \code{focusElement()}.}

\item{outputId}{Graph output id. When set (and running under Shiny), clicking
a row sets \verb{input$<outputId>-outlined_element} to a list with \code{id}, \code{type} and
\code{label}.}

\item{...}{Additional parameters passed to the plugin configuration.}
}
\value{
A list with the configuration for the outline plugin.
}
\description{
Renders a panel over the canvas listing the graph as a tree: each combo is an
accordion holding its members, and clicking a row moves the viewport to that
element. Where \code{\link[=g6_search]{g6_search()}} answers "take me to the thing I am looking for",
an outline answers "show me what is in here", which is the question a large
graph usually raises.
}
\details{
It also makes collapsing the canvas safe: with everything listed, the drawing
no longer has to be legible at fit-to-view, so groups can stay collapsed and
any element is still two clicks away.

Folding a group in the panel is independent of collapsing it on the canvas, so
the list can be explored without redrawing the graph. Selecting an element on
the canvas marks and scrolls to its row, so the panel follows where you are.
}
\examples{
config <- g6_outline()

# Start folded, and name the types the way the app does
config <- g6_outline(
  groupsOpen = FALSE,
  labels = c(node = "block", combo = "stack"),
  outputId = "graph"
)
}
\seealso{
\code{\link[=g6_search]{g6_search()}} to jump straight to a named element.
}
