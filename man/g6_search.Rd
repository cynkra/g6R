% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plugins.R
\name{g6_search}
\alias{g6_search}
\title{Search box to navigate a large graph}
\usage{
g6_search(
  key = "search",
  placeholder = "Search",
  limit = 8,
  elements = c("node", "combo"),
  expandAncestors = TRUE,
  select = TRUE,
  position = c("top-left", "top-right", "bottom-left", "bottom-right"),
  labels = c(node = "node", combo = "combo", edge = "edge"),
  width = 220,
  animation = NULL,
  outputId = NULL,
  onSelect = NULL,
  ...
)
}
\arguments{
\item{key}{Unique identifier for the plugin (string).}

\item{placeholder}{Placeholder shown in the empty box.}

\item{limit}{Maximum number of matches listed.}

\item{elements}{Element types to search, any of \code{"node"}, \code{"combo"} and
\code{"edge"}. Edges are rarely worth searching, so they are off by default.}

\item{expandAncestors}{Expand collapsed combos on the way to a match. A node
inside a collapsed combo has nowhere on screen to focus, so leaving this on
is what makes search useful on a board whose groups are collapsed.}

\item{select}{Also select the picked element, so anything driven by selection
(a sidebar, a details panel) follows the search.}

\item{position}{Corner to render in: \code{"top-left"} (default), \code{"top-right"},
\code{"bottom-left"} or \code{"bottom-right"}.}

\item{labels}{How to name each element type in the results, as a named
character vector over \code{"node"}, \code{"combo"} and \code{"edge"}. \code{"combo"} is g6
jargon, so an app that calls them something else ("stack", "stage", "group")
should say so here. Only used where a match has no group to show instead.}

\item{width}{Width of the box, in px.}

\item{animation}{Viewport animation passed to \code{focusElement()}, e.g.
\code{list(duration = 500, easing = "ease-in")}. \code{NULL} uses G6's default.}

\item{outputId}{Graph output id. When set (and running under Shiny), picking a
match sets \verb{input$<outputId>-searched_element} to a list with \code{id}, \code{type} and
\code{label}.}

\item{onSelect}{Optional \code{\link[=JS]{JS()}} callback \verb{(hit, graph) => \{\}} run after the
viewport has moved, for anything beyond focus and select.}

\item{...}{Additional parameters passed to the plugin configuration.}
}
\value{
A list with the configuration for the search plugin.
}
\description{
Renders a search box over the canvas: type to match nodes and combos by
label or id, pick one, and the viewport moves to it. G6 ships no search UI,
so \code{g6_search()} adds one on top of \code{\link[=g6_focus_nodes]{g6_focus_nodes()}}'s underlying
\code{focusElement()}.
}
\details{
Matching runs in the browser, so the box works in a plain widget (a Quarto
document, a pkgdown page, a vignette) and not only in a Shiny app, and it does
not wait on the server between keystrokes. Under Shiny the pick is also
reported as an input when \code{outputId} is given.
}
\examples{
# Nodes and combos, focusing the pick
config <- g6_search()

# Report the pick to Shiny, and keep collapsed groups closed
config <- g6_search(
  outputId = "graph",
  expandAncestors = FALSE,
  position = "top-right"
)

# Name the element types the way the app does
config <- g6_search(labels = c(node = "block", combo = "stack"))
}
